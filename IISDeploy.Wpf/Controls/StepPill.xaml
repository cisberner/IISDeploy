<UserControl x:Class="IISDeploy.Wpf.StepPill"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
        <Border x:Name="Circle" Width="28" Height="28" CornerRadius="14"
                Background="{StaticResource SurfaceAltBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <TextBlock x:Name="NumberText" Text="1" FontFamily="Segoe UI Semibold" FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
                <TextBlock x:Name="CheckText" Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="Collapsed" />
            </Grid>
        </Border>
        <TextBlock x:Name="CaptionText" Text="Step" FontFamily="Segoe UI Semibold" FontSize="12.5"
                   Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="9,0,0,0" />
    </StackPanel>
</UserControl>
